import { ProfanityBlockerDirective } from './profanity-blocker.directive';

describe('ProfanityBlockerDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfanityBlockerDirective();
    expect(directive).toBeTruthy();
  });
});
