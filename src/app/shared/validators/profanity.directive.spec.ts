import { ProfanityDirective } from './profanity.directive';

describe('ProfanityDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfanityDirective();
    expect(directive).toBeTruthy();
  });
});
